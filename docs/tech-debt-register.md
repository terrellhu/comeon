# Tech Debt Register

Tracked advisory deviations and known issues from story completion reviews.
Each entry includes the date, story, description, and source story path.

---

- **2026-06-03** (Jump System — Coyote Time + Jump Buffer): Integration tests for 6/7 ACs (AC-jump, AC-coyote-success, AC-coyote-expired, AC-buffer-success, AC-buffer-expired, AC-no-double-jump) deferred to `game/tests/integration/player_controller/test_pc_jump_integration.gd` — pending() stubs in unit test file document per-AC sub-assertions. Create before sprint close-out QA. — tracked from `production/epics/player-controller/story-002-pc-jump.md`
- **2026-06-03** (Jump System — Coyote Time + Jump Buffer): Jump buffer fires via `_transition_to(AIRBORNE)` self-transition (AIRBORNE→AIRBORNE at `player_controller.gd` buffer-fire block). Harmless today (enter_state is `pass`); will restart jump animation when Story 005 wires `_enter_state(AIRBORNE)`. Story 005 author must replace `_transition_to(AIRBORNE)` with direct `velocity.y = -jump_impulse` at that code path. — tracked from `production/epics/player-controller/story-002-pc-jump.md`
- **2026-06-03** (Jump System — Coyote Time + Jump Buffer): Horizontal velocity not applied on jump frame — `_handle_input()` early return skips Priority 5 horizontal move code. Previous frame's velocity.x carries over. ADR does not specify; gameplay programmer should document the intended behaviour. — tracked from `production/epics/player-controller/story-002-pc-jump.md`
- **2026-06-03** (HitpauseManager Autoload + Runtime Verification): `trigger_hitpause(duration <= 0.0)` 입력 방어 코드 없음 — 음수 duration 전달 시 `_active=true` 고착으로 `Engine.time_scale=0` 영구 지속 가능. 호출자 책임으로 간주하나, 명시적 assert 또는 clamp 추가 권장. — tracked from `production/epics/retry-context/story-002-hitpause-manager-autoload.md`
- **2026-06-03** (HitpauseManager Autoload + Runtime Verification): GUT 테스트 `test_time_scale_set_to_zero_on_trigger`에서 `trigger_hitpause(999.0)` await 없이 호출로 SceneTreeTimer가 잔류해 "8 unfreed children" 경고 발생. 기능 영향 없으나 CI 로그 노이즈. 테스트 재설계 또는 주석 추가로 의도 명시 권장. — tracked from `production/epics/retry-context/story-002-hitpause-manager-autoload.md`
- **2026-06-03** (Parry Signal Contract): `exit_parry_state()` adds a `duration <= 0.0` guard (`push_warning` + early return) not specified in the GDD. Defensive programming; prevents timer lock-forever on bad caller input. If ParryTelegraphSystem is always trusted, this guard can be removed — otherwise document the minimum valid duration in the GDD. — tracked from `production/epics/player-controller/story-003-pc-parry.md`
- **2026-06-03** (Parry Signal Contract): AC-parry-priority (same-frame parry+dodge) and AC-parry-isolation-jump are deferred to `game/tests/integration/player_controller/test_pc_parry_physics.gd` (does not yet exist). Create this physics-scene integration test before sprint QA close-out to achieve full AC coverage. — tracked from `production/epics/player-controller/story-003-pc-parry.md`
- **2026-06-03** (HIT_STUN + DEAD State — Damage Response): `reset_for_retry(_ctx: Dictionary) -> void` skeleton stub added to `player_controller.gd` during Story 004 to fix a malformed `##` doc comment left from Story 001 and to skeletonise the ADR-0003 contract. Story 006 must replace the `pass` stub with the full implementation per ADR-0003 (resetting player_state, velocity, position, facing_direction, all timers, and `_prev_hp` to post-retry player HP). — tracked from `production/epics/player-controller/story-004-pc-hit-stun.md`
- **2026-06-05** (BSM Story 001 — BSM Skeleton + IDLE/TELEGRAPHING/ATTACKING Main Path): `@export var _anim_player` uses underscore prefix — Godot 4 convention is that `@export` vars should not be prefixed with underscore (underscore signals "private", but @export is inherently public). Rename to `anim_player` in a future cleanup. — tracked from `production/epics/boss-state-machine/story-001-bsm-skeleton.md`
- **2026-06-05** (BSM Story 001 — BSM Skeleton + IDLE/TELEGRAPHING/ATTACKING Main Path): Missing unit test for `_pending_anim_fallback` cleanup in `_exit_state(ATTACKING)`. Test scaffold: force-exit from ATTACKING (with `_anim_player = null`) and assert `_pending_anim_fallback == false` after. Must be added before Story 002 (STAGGERED interrupts ATTACKING). — tracked from `production/epics/boss-state-machine/story-001-bsm-skeleton.md`
- **2026-06-05** (BSM Story 001 — BSM Skeleton + IDLE/TELEGRAPHING/ATTACKING Main Path): `_event_bus: Node` duck-typing requires `@warning_ignore("unsafe_property_access")`. Long-term improvement: define `class_name EventBusBase extends Node` with all signals, let `EventBus` and `MockEventBus` both extend it. Eliminates warning suppression and adds compile-time type safety. — tracked from `production/epics/boss-state-machine/story-001-bsm-skeleton.md`
